//import solidity
pragma solidity ^0.4.0;

contract Simple{
    //ether sazbo finney wei
    bool isEquial = (1 ether == 5000 finney);

    //time units
    bool isTime = (1 == 1 seconds);

    //seconds, minutes, hours, days, weeks always in plural
    bool isTime = (1hours == 60 minutes);
}
